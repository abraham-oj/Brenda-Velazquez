---
interface Props {
  title: string;
}

const { title } = Astro.props;
---

<!doctype html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="description" content="Sitio oficial de Brenda Velázquez" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <title>{title}</title>
  </head>
  <body>
    <slot />
  </body>
</html>

<style is:global>
  /* 🛡️ RESET SUPREMO - Abraham, esto va a tu Vault de Obsidian */

  /* 1. Quita todos los márgenes y rellenos por defecto */
  *,
  *::before,
  *::after {
    box-sizing: border-box;
    margin: 0;
    padding: 0;
  }

  /* 2. Control total del cuerpo */
  html,
  body {
    width: 100%;
    max-width: 100vw; /* Evita que crezca más allá de la pantalla */
    overflow-x: hidden; /* MATA el scroll horizontal (eterno scroll) */
    background-color: #f5f5f5; /* Un gris suave, mejor que blanco nuclear */
    font-family: system-ui, sans-serif;
  }

  /* 3. Imágenes responsivas por defecto */
  img,
  video {
    max-width: 100%;
    height: auto;
    display: block; /* Elimina el espacio blanco debajo de las imágenes */
  }
</style>
