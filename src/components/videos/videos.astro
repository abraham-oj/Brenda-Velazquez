---
const videos = [
  { src: "/videos/campaign-video-1.mp4", titulo: "Mensaje a la Base" },
  { src: "/videos/campaign-video-2.mp4", titulo: "Recorrido Sindical" },
  { src: "/videos/campaign-video-3.mp4", titulo: "Nuestras Propuestas" },
  { src: "/videos/campaign-video-4.mp4", titulo: "La Fuerza Verde" },
];
---

<section id="videos" class="video-section">
  <div class="container">
    <h2 class="section-title">Mensajes de Viva Voz</h2>
    <p class="section-subtitle">
      Escucha nuestras propuestas y siente la energía del movimiento.
    </p>

    <div class="video-grid">
      {
        videos.map((video) => (
          <div class="video-card">
            <div class="video-wrapper">
              <video controls preload="metadata">
                <source src={video.src} type="video/mp4" />
                Tu navegador no soporta video HTML5.
              </video>
            </div>
            <div class="video-caption">
              <h3>{video.titulo}</h3>
            </div>
          </div>
        ))
      }
    </div>
  </div>
</section>

<style>
  .video-section {
    padding: 5rem 2rem;
    background: #ffffff; /* Fondo limpio */
  }

  .video-grid {
    display: grid;
    grid-template-columns: repeat(
      auto-fit,
      minmax(450px, 1fr)
    ); /* 2 columnas en PC */
    gap: 2rem;
    margin-top: 3rem;
  }

  .video-card {
    background: white;
    border-radius: 15px;
    overflow: hidden;
    box-shadow: 0 10px 30px rgba(0, 77, 37, 0.15); /* Sombra verde oscura */
    border: 1px solid #e2e8f0;
    transition: transform 0.3s ease;
  }

  .video-card:hover {
    transform: translateY(-5px);
    box-shadow: 0 15px 40px rgba(118, 188, 33, 0.25); /* Sombra verde claro al hover */
  }

  .video-wrapper {
    position: relative;
    width: 100%;
    padding-top: 56.25%; /* Aspect Ratio 16:9 (Youtube style) */
    background: black;
  }

  video {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .video-caption {
    padding: 1.5rem;
    background: linear-gradient(to right, #f8fcf4, #ffffff);
    border-top: 4px solid var(--color-primary); /* Línea verde decorativa */
  }

  .video-caption h3 {
    margin: 0;
    color: var(--color-secondary); /* Verde oscuro */
    font-size: 1.2rem;
    text-align: center;
  }

  @media (max-width: 768px) {
    .video-grid {
      grid-template-columns: 1fr; /* 1 columna en móvil */
    }
  }
</style>
